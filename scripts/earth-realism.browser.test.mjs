import {createRequire} from 'node:module';import assert from 'node:assert/strict';import {missions} from '../src/scripts/sample-missions/data.js';
const require=createRequire(import.meta.url),{chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');const browser=await chromium.launch({headless:true,executablePath:process.env.EDGE_EXECUTABLE});
try{const page=await browser.newPage({viewport:{width:1440,height:1000},reducedMotion:'reduce'}),errors=[];page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error')errors.push(m.text());});await page.addInitScript(()=>sessionStorage.setItem('mads-cosmic-arrival-v1','done'));await page.goto((process.env.SITE_TEST_URL||'http://127.0.0.1:52523')+'/research.html');const root=page.locator('[data-sample-missions]'),view=root.locator('[data-mission-viewport]');await view.scrollIntoViewIfNeeded();const ready=()=>page.waitForFunction(()=>sampleMissions?.state.ready,null,{timeout:60000});await ready();
for(const id of ['hayabusa2','osiris-rex']){await page.evaluate(id=>sampleMissions.choose(id),id);await ready();for(const width of [1440,390]){await page.setViewportSize({width,height:1000});for(const [kind,p] of [['launch',0],['launch',.18],['launch',.55],['launch',.75],['launch',.95],['return',0],['return',.18],['return',.5],['return',.95],['landing',0],['landing',.15],['landing',.65],['landing',.8],['landing',1]]){const stage=missions[id].stages.findIndex(s=>s.kind===kind);await page.evaluate(({stage,p})=>sampleMissions.select(stage,p),{stage,p});await view.scrollIntoViewIfNeeded();await page.waitForTimeout(180);const state=await page.evaluate(()=>sampleMissions.state);assert(state.earth.altitudeKm>=-.000001);assert(state.calls>0);assert(await root.locator('[data-mission-inset]').isVisible());await root.locator('.mission-scene').screenshot({path:process.env.TEMP+`/earth-real-${id}-${kind}-${p}-${width}.png`});}assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1));}}
assert.deepEqual(errors,[]);console.log('PASS physical Earth launch/separation/entry frames, insets, nonnegative altitudes, 1440/390 and no shader/page errors');}finally{await browser.close();}
