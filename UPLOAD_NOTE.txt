Replace this file only:

assets/css/style.css

Change:
- Block depth contrast is increased.
- Large outer cards are darker/deeper in Space Mode.
- Inner information blocks are lighter and more distinct.
- Small chips/date tags are the brightest layer.
- Navigation thin glowing underline is untouched.
