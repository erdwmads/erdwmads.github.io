Replace/upload these files:

assets/css/style.css
assets/js/interface-2046.js
assets/js/mission-lightbox.js

Fix:
- Reverts the raster-safe background implementation that changed the visual feel.
- Uses real fixed background layers on mobile instead of fixed body pseudo-elements.
- Keeps blur, ambient layer, glass cards, and lightbox blur.
